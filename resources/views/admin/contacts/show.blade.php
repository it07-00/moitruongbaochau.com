@extends('admin.layouts.app')
@section('title', $contact->name)
@section('content')
<h1 class="text-3xl lg:text-4xl font-bold mb-8">{{ $contact->name }}</h1><div class="grid lg:grid-cols-3 gap-6"><section class="lg:col-span-2 bg-white rounded-2xl border border-gray-200 p-6 space-y-4"><p><strong>Điện thoại:</strong> {{ $contact->phone }}</p><p><strong>Email:</strong> {{ $contact->email ?: 'Không cung cấp' }}</p><p><strong>Chủ đề:</strong> {{ $contact->topic ?: 'Tư vấn chung' }}</p><div><strong>Nội dung:</strong><p class="mt-2 whitespace-pre-line">{{ $contact->message }}</p></div></section><aside class="bg-white rounded-2xl border border-gray-200 p-6"><form method="post" action="{{ route('admin.contacts.update', $contact) }}" class="space-y-4">@csrf @method('patch')<label for="status" class="block font-bold">Trạng thái</label><select id="status" name="status" class="w-full rounded-xl border border-gray-300 px-4 py-3">@foreach(\App\ContactStatus::cases() as $status)<option value="{{ $status->value }}" @selected($contact->status === $status)>{{ str($status->value)->headline() }}</option>@endforeach</select><button type="submit" class="bg-primary text-white rounded-full px-5 py-2.5 font-bold">Cập nhật</button></form></aside></div>
@endsection
