@extends('admin.layouts.app')
@section('title', 'Danh mục')
@section('content')
<h1 class="text-3xl lg:text-4xl font-bold mb-8">Danh mục {{ $type === 'services' ? 'dịch vụ' : 'bài viết' }}</h1>
<div class="grid lg:grid-cols-3 gap-6"><form method="post" action="{{ route('admin.categories.store', $type) }}" class="bg-white rounded-2xl border border-gray-200 p-6 space-y-4">@csrf<h2 class="text-xl font-bold">Thêm danh mục</h2><div><label for="name" class="block font-bold mb-2">Tên *</label><input id="name" name="name" value="{{ old('name') }}" required class="w-full rounded-xl border border-gray-300 px-4 py-3">@error('name')<p class="text-red-600 text-sm">{{ $message }}</p>@enderror</div><div><label for="slug" class="block font-bold mb-2">Slug</label><input id="slug" name="slug" value="{{ old('slug') }}" class="w-full rounded-xl border border-gray-300 px-4 py-3"></div><div><label for="description" class="block font-bold mb-2">Mô tả</label><textarea id="description" name="description" rows="4" class="w-full rounded-xl border border-gray-300 px-4 py-3">{{ old('description') }}</textarea></div><label class="flex items-center gap-2"><input type="checkbox" name="is_active" value="1" checked> Kích hoạt</label><button class="bg-primary text-white rounded-full px-5 py-2.5 font-bold">Thêm danh mục</button></form><div class="lg:col-span-2 bg-white rounded-2xl border border-gray-200 overflow-hidden"><table class="w-full text-left"><thead class="bg-gray-50"><tr><th class="p-4">Tên</th><th class="p-4">Slug</th><th class="p-4"></th></tr></thead><tbody>@forelse($categories as $category)<tr class="border-t border-gray-100"><td class="p-4 font-semibold">{{ $category->name }}</td><td class="p-4">{{ $category->slug }}</td><td class="p-4 text-right"><form method="post" action="{{ route('admin.categories.destroy', [$type, $category->id]) }}">@csrf @method('delete')<button class="text-red-600 font-bold">Xóa</button></form></td></tr>@empty<tr><td colspan="3" class="p-8 text-center">Chưa có danh mục.</td></tr>@endforelse</tbody></table></div></div><div class="mt-6">{{ $categories->links() }}</div>
@endsection
